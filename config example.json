{
    "collected_tools_id": [
        "example_tool"
    ],
    "costom_tools": [
        {
            "id": "example_tool",
            "if": "//JS conditions code",
            "command": "cmd command here",
            "key": "键盘上的按键，只要不是CapsLock之类的应该都行，可省略该键值"
        }
    ],
    "plugin_tools": [
        {
            "id": "example_plugin",
            "path": "plugin_folder/plugin.py",
            "json_command": ["window_name"],
            "key": "1"
        }
    ],
    "default_plugin_settings": {
        
    },
    "app_settings": {
        "panel_place": "bottom",
        "Chinese": true
    }
}